[phases.setup]
nixPkgs = ["python3", "gcc"]

[variables]
PLAYWRIGHT_BROWSERS_PATH = "/app/.playwright-browsers"

[phases.install]
cmds = [
    "pip install -r requirements.txt",
    "PLAYWRIGHT_BROWSERS_PATH=/app/.playwright-browsers playwright install chromium --with-deps"
]
